// ============================================================
// MODO MANTENIMIENTO
// ============================================================
// Para poner el sitio en mantenimiento: cambiar a `true`.
// Para volver a activar el sitio normal: cambiar a `false`.
// Después de cambiar este valor hay que hacer commit + push
// (o `ng build` y volver a desplegar) para que el cambio
// se refleje en producción.
// ============================================================
export const MAINTENANCE_MODE = false;
